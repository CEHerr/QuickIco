namespace WinSysUtil {
    public static class SystemLiaison {
        //<summary>
        //this class translates requests from the program into actual system calls
        //this centralized point of contact with the system allows the monitoring
        //of outgoing requests and incoming data
        //and the changing of the specific implimentation of system calls in one
        //location
        //</summary>

        /*
        public string[] GetFiles(string folderPath);

        public string[] GetSubDirs(string folderPath);

        public void AddAttributes(string path, FileAttributes[] attributes);
        public void RemoveAttributes(string path, FileAttributes[] attributes);

        public void OverwriteTextFile(string path, string content);
        */
    }
}
